# PyTorch and Deep Learning
# CUDA 12.1 지원 버전 (GPU 사용 시)
torch>=2.5.1  # CUDA 12.1+ 지원 버전으로 설치: pip install torch --index-url https://download.pytorch.org/whl/cu121
transformers==4.57.3
accelerate==1.12.0

# Hugging Face ecosystem
huggingface-hub==0.36.0
safetensors==0.7.0
tokenizers==0.22.2

# Core dependencies
numpy>=2.4.0

# FastAPI (core service)
# Note: Also see apps/core/requirements.txt for core service specific dependencies
fastapi==0.111.0
uvicorn[standard]==0.30.1
sse-starlette==1.6.5

